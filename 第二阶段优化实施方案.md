# 第二阶段优化实施方案：测量验证、带宽扩展与性能提升

编制日期：2026-09-20。

状态：**待实施方案**。本文没有将计划目标计入已完成成绩。本次仅新增方案文档，不改变板上程序、SD启动文件或现有验收结果。

## 1. 目标、依据与范围

依据项目外部文件 `D:\fft\赛道2_FPGA设计及应用赛题整理.md`，比赛要求分析数字基带信号的幅度、帧长度、频点和带宽，完成RTL、前仿真及FPGA板测。四项明确门槛为：I/Q位宽≥12bit、速率≥100MSPS、FFT≥8192点、分析时间≤2ms。带宽没有明确下限；速率更高、带宽更大、处理时间更短属于性能竞争方向。

本轮目标依次为：补齐测量误差与适用范围证据；扩大可验证的信号带宽；改善整体时序；评估降低延迟；最后独立评估125MSPS。

继续使用Zybo Z7-20、现有电脑、SD卡、USB和网线。电脑提供I16/Q16数据，FPGA板内回放，不增加ADC、射频前端或新开发板。保留AMD FFT IP，明确其与自研模块的边界。题面没有明确禁止IP或板内回放；若有赛方补充规则，单独核对，不能将本方案解释视为官方认可。

当前事实以[本轮优化验收报告](reports/本轮优化验收报告.md)和[冷启动验证报告](reports/冷启动验证报告.md)为准。本文承接前一阶段T08～T10并细化；前一阶段方案及已删除的文档可从Git历史查阅，当前发布只保留代码、验收报告和本方案。

## 2. 冻结基线与当前差距

以Git提交 `5e7eef2af407fc60af0b940c6439f34dfb774da4` 为本轮开始前的软件、报告基线，硬件版本为 `0x00010001`。该提交包含冷启动补充记录。

| 项目 | 已验证事实 | 第二阶段方向 |
|---|---|---|
| 输入与FFT | I/Q各16位，板内100MSPS，8192点FFT | 前期保持不变 |
| 分析/发布延迟 | 完整套件最大199.79/200.13µs | 先测阶段耗时，再决定优化 |
| 数字零测长 | 已选8组完整波形起止及长度误差0点 | 扩展边界与幅度；不推广为带噪通用分帧 |
| 门限测长 | 与参考算法一致；矩形突发24576点测得24591点 | 量化不同噪声、幅度、间隔下的实际误差 |
| 频率/幅度 | ±25MHz单音、半幅输入等已测 | 扫频、非整数频点、幅度扫描 |
| 已测带宽 | QPSK、Hann窗，99%占用带宽54.663086～54.895020MHz | 尝试更宽信号，报告实际值与误差 |
| 时序 | setup +0.030ns，hold +0.004ns | 改善整体裕量；不直接提频 |
| 资源 | LUT12359、FF20030、BRAM90.5/140、DSP53/220 | BRAM约64.64%，硬件并行化需核对剩余资源 |
| 实板 | 32组有限、4组连续，1708622条频域及427153条突发通过 | 保留并作为硬件修改后的回归 |
| 冷启动 | 用户确认物理断电上电；之后纯网口三项采集通过 | 新启动产物替换后重新验证 |

`release/validated_20260920.zip`保留为冻结交付包；它生成于冷启动补充验证之前。另行归档该提交的文档、冷启动原始记录及散列，不修改旧包去补写历史状态。实体SD上的旧BOOT备份 `BE845539.BIN` 不保证就是本轮基线版本；回退前必须以目标BOOT的SHA-256确认身份。

## 3. 实施阶段与决策门槛

| 阶段 | 优先级 | 主要任务 | 主要交付物 | 估计工作量 |
|---|---|---|---|---|
| S0 | P0 | 固定基线、定义测量口径、隔离实验 | 基线清单、样例清单格式 | 0.5人日 |
| S1 | P0 | 参数化输入与独立参考、验证频率/幅度 | 通用参考接口、误差表、测试记录 | 1～2人日 |
| S2 | P0 | 带噪/偏置帧长与门限适用范围 | 检出率、误差与条件表 | 1～2人日 |
| S3 | P1 | 60/70/80MHz候选宽带波形验证 | 带宽扫描、最宽稳定档实板报告 | 1～2人日 |
| S4 | P1 | 布线后时序诊断与小步优化 | 路径、资源、时序前后对比 | 1～3人日 |
| S5 | P2 | 分阶段延迟测量、可选扫描优化 | 时延分解、候选实现或不实施结论 | 1～3人日 |
| S6 | P2可选 | 125MSPS独立探索 | 完整参数化与新档位验收，或失败分析 | 2～4人日 |
| S7 | P0交付 | 回归、启动验证、参赛材料与打包 | 新交付包、指标表、演示材料 | 1人日以上 |

估时为工程工作量，不承诺日历完成时间；不包括等待人工断电、照片和视频。优先交付S0～S3及对应S7；S4～S6可分别形成后续候选版本，不以提频成功作为整个工程交付条件。

执行关系：`S0 → S1 → S2 → S3 → 测量能力交付`；随后 `S4 → S5 → 可选S6 → 硬件版本交付`。每阶段先完成输入/参考与有限测试，再扩展连续测试，避免全参数笛卡尔积造成大量低价值运行。

## 4. S0：基线与实验隔离

1. 记录当前Git提交、工具版本、实际FFT配置、bit/XSA/ELF/BOOT散列、原始板测目录。
2. 在独立分支或独立工作目录开展实验。构建输出仍按现有脚本布局生成，但不得覆盖唯一保留的基线产物和数据。
3. 原8种向量和16种窗组合的黄金数据保持不变。新样例放入独立数据子目录，不追加到旧清单后直接假定现有SD固件和仿真循环会自动支持。
4. 新采集使用唯一目录；报告分清基线、候选、通过和失败。失败数据保留用于定位，不混入正常性能统计。
5. 新实验报告绑定输入、参考、脚本、配置和产物散列。改变同名文件后不得继续沿用旧PASS。
6. 新增到`tests/`、`host/`的文件会改变当前仿真来源清单；修改后按影响范围重新执行仿真记录流程，不通过删除来源检查或手工重写旧报告绕过门槛。

当前100MSPS指一对I/Q每10ns进入分析电路。板内最大回放长度32768对，约327.68µs；长时间采集重复该段数据。与电脑网口持续输入400MB/s新数据的能力分开描述。

## 5. S1：测量参考与误差验证

### 5.1 先修补测试工具的能力边界

目前`host/iq_client.py`可装载8192/16384/24576/32768对I16/Q16；但`verify_board_capture.py`依赖现有黄金用例、固定默认门限、100MSPS和特定循环接缝。它不是任意波形校验器。

实施顺序：

1. 从`tests/make_golden.py`提取可调用的AMD FFT位精确模型接口；保留原脚本默认行为与旧结果。
2. 新样例由独立清单驱动，每例记录采样率、点数、窗函数、检测模式、门限参数、随机种子、幅度、频率、设计帧区间、噪声/偏置和SHA-256。
3. 给新增校验入口传入明确的样例清单和参考结果；不靠文件名猜测真值。不支持的采样率、参数或循环方式必须明确报错。
4. 按实际8192点窗口生成参考。量化、Hann系数、舍入、FFT缩放、溢出、功率累加、整数99%分位阈值及频率换算必须一致。
5. 分开输出两种结果：硬件是否符合位精确参考；算法相对信号设计目标的误差。NumPy浮点FFT只作算法分析参考，不替代AMD位精确模型证明硬件一致性。
6. 原模式、原16组合与原黄金数据通过回归后，才允许批量新样例板测。

### 5.2 频率测试矩阵

固定Fs=100MHz、N=8192，频点间隔Δf=Fs/N=12,207.03125Hz。

| 类别 | 建议输入 | 核对重点 |
|---|---|---|
| 整数频点单音 | 选择对应约−45、−25、−5、0、+5、+25、+45MHz的整数k | 输入频率用k·Fs/N，峰值bin与位精确参考完全一致 |
| 非整数频点 | 以低、中、高频三个k为中心，偏移0.1/0.25/0.49个bin；正负频率 | 频谱泄漏、窗函数影响、峰值量化误差 |
| 接近频带边缘 | ±47、±49MHz，保持输入不越界 | EDGE_RISK、ROI边界，不把环绕带宽当普通带宽 |
| 双音与同峰 | 相隔1/2/8/64个bin，等幅和不等幅 | 分辨能力、峰值并列选择规则；不承诺1bin总能分开 |
| 零输入 | I=Q=0 | NO_POWER与结果定义正确，不计入正常频率误差统计 |

主测试用幅度8192；矩形/Hann窗分别运行。设计频率、量化波形的浮点谱峰、位精确参考和实板输出四者分别记录。

工程目标：正常高信噪比单音的谱峰误差争取≤1个bin；孤立非整数单音另检查是否符合最近bin量化预期。该目标不是赛题官方精度门槛，不用于约束双音干扰或低信噪比输入。没有亚bin估计时不宣传任意频率均可达到1Hz精度。

### 5.3 幅度与定点范围

- 主扫描幅度：256、512、1024、4096、8192、16384、30000码值；1/2/16/64码值作为低幅边界，独立标记可能低于门限或FFT量化能力。
- 输入类别：复单音、QPSK、矩形突发；增加I或Q单独非零、两路负满幅等边界。
- 所有常规输入写入int16前检查溢出，不通过裁剪悄悄改变信号。刻意削顶样例单独分类。
- 峰值定义为max(sqrt(I²+Q²))，RMS为sqrt(mean(I²+Q²))；说明统计区间是8192点窗口还是检测到的突发。
- 数字码值与物理电压分开。复包络峰值可能超过单路32767；若以后显示dBFS，必须先定义参考值，不能直接把双路满幅等同单路满幅。
- 实板峰值、RMS和能量应与定点参考逐项一致；同时统计相对量化输入和原浮点输入的误差。零幅度只计算绝对误差，避免除零。

交付表字段：样例、输入参数、窗、实际峰值/RMS、参考值、绝对误差、相对误差、状态标志、原始采集路径。输出每类最大值和中位数；大量样例时增加P95，不能只展示最有利的样例。

## 6. S2：噪声、偏置与帧长适用范围

### 6.1 输入与真值

1. 保持现有数字零模式用于精确零背景；噪声/偏置场景主要评估门限模式。数字零模式失去有效分帧时记录为不适用，不修改定义使其假装通过。
2. 长度选64、512、4096、24576点，起点覆盖FFT边界前后，间隔覆盖确认点数附近及充足间隔。
3. QPSK分别保存符号帧区间、成形滤波输出区间、量化后非零区间；滤波尾部不与协议帧边界混称。
4. AWGN候选SNR为30/20/10/5/0dB；SNR按加入噪声前的有效信号区间定义。保存量化前和量化后的实测SNR，不能把命令参数当作最终值。
5. 每个重点噪声工况先用3个固定seed；需要统计检出率时累计至少100个已知突发并注明样本数。重复同一段噪声100次不等于100个独立噪声实现。
6. 直流偏置候选(I,Q)为(0,0)、(32,0)、(128,128)、(512,−512)码值；与不同幅度的组合先检查int16范围。

第一轮先选幅度8192、长度4096及30/20/10dB定位失效边界；随后补短帧、低幅和更低SNR，避免一次运行全部组合。

### 6.2 检测评价方法

对参考帧区间和检测区间作确定的一对一匹配，预先固定重叠/边界容差与并列处理规则。分别计数正常匹配、漏检、虚警、误合并、误拆分；不能只对成功检出的帧统计长度。

报告检出率、虚警数/背景观测时间、起止偏差、长度偏差，单位同时用点与µs；100MSPS下1点=10ns。SNR、幅度、门限、确认长度和seed必须随结果保存。

硬性验收是硬件与声明算法一致、失败被显式识别、统计完整；对所有SNR不预设“必须0点误差”。无噪完整零背景样例继续要求数字零模式起止和长度0点误差。

### 6.3 门限优化决策

当前CLI把部分门限和确认参数固定在配置数组中。先审查寄存器、固件支持范围，再在需要时增加受范围检查的CLI/GUI参数，并保存实际配置读回；不在GUI增加一个实际上未写入硬件的设置。

优先评估固定参数分档和基于已知前导静默区的噪声估计。调参用训练seed，验收用独立seed；禁止对每个测试帧用真值反向挑选最优门限后宣称自动检测。

只有固定参数确实无法满足选定演示场景，才立项自适应门限。新增算法须明确噪声估计窗口、无静默区行为、迟滞和开销，独立仿真及板测后才能进入默认版本。协议前导检测仍为可选新功能，不能由本方案暗中扩大为必做任务。

## 7. S3：扩大实际可验证带宽

### 7.1 指标口径

成绩使用整个8192点硬件谱计算的99%占用带宽，不能用1024点显示快照估计替代。RRC理想支撑宽度约(1+α)Rs、符号率、设定目标和实测99%带宽分别记录。

Fs=100MSPS时复频轴为[−50,+50)MHz，并不等于已经测得100MHz有效信号带宽。当前CDF算法在普通线性频轴上计算，不支持跨Nyquist边界的环绕最短占用带宽；此类样例只作异常/适用范围验证，不计入扩大带宽成绩。

### 7.2 波形生成与逐档验证

1. 保留当前约54.9MHz QPSK作对照，设置60/70/80MHz候选目标；目标不等于最终承诺。
2. 当前生成器主要依赖整数SPS。以2点/符号、Rs=50Msym/s为起点，扫描RRC滚降系数约0.2/0.4/0.6，对应理想支撑约60/70/80MHz，再根据实际99%带宽调整。若需任意符号率，另实现并验证有理重采样，不强塞入原整数SPS公式。
3. 固定seed，保存成形滤波长度、归一化方式、PAPR和峰值余量；先消除正常样例的裁剪与FFT溢出。
4. 每档先做居中波形、矩形/Hann窗各3个seed。有限突发保留充分前后静默区，连续回放的接缝必须明确设计。
5. 参考必须使用实际量化后的32768点文件；四个8192点窗口分别比较，标明稳态窗及包含突发边沿的窗。不得只挑最宽的一帧作代表。
6. 频移测试根据带宽留保护带，满足中心频率加减半带宽不过Nyquist边缘。越界用例独立标记。
7. 每档有限验证通过后做10秒连续；最宽稳定档再做60秒连续，逐条检查数值、记录完整性、时序和错误标志。

重复回放只证明同一输入块的持续处理稳定性。用不同seed重新上传的多次有限采集补足信号多样性，不能把60秒循环说成60秒独立随机数据。

验收：正常样例全部位精确一致，无未解释的丢样/溢出，99%带宽定义不变。报告每档min/median/max、seed数、窗口数、边沿情况。某档失败则降低目标或修复，不修改带宽定义来扩大成绩。

## 8. S4：时序优化

### 8.1 当前结论

之前ROI/峰值局部路径由0.109ns改善到峰值端点1.016ns，但全局最差转移到FFT内部高扇出路径，setup降至0.030ns。不能通过再次增加ROI流水就假定全局瓶颈会消失。

### 8.2 执行步骤

1. 读取实际布线后的全局最差路径、FFT内部路径、复位/使能高扇出网络、setup/hold、CDC和未约束端点。
2. 区分逻辑级数、布线延迟和高扇出负载，记录准确起止点与时钟域。
3. 一次只试一个实现策略或局部扇出优化，先保持100MHz采样与125MHz FFT时钟。使用工具支持的实现策略，不修改生成的厂商IP网表。
4. 外部复位/使能若确为关键因素，先证明复位同步、极性和启动行为等价，再考虑同步寄存器复制等调整。
5. 原时钟约束保持真实；不能扩大周期、添加无依据的false path或multicycle约束来掩盖失败。
6. 最终候选完成完整实现，记录资源、setup、hold、CDC、DRC及板测结果；实现策略实验的选择必须可重复。

必须条件：setup/hold均非负、无未约束内部端点、CDC Critical=0、功能一致。争取setup≥0.10ns并兼顾hold，但这是内部期望，不是比赛门槛或成功承诺。

若全局裕量、资源或吞吐出现新的明显退化，保留现有稳定版本并提交失败分析；不以仅某条路径变好替代整体判断。

## 9. S5：分析延迟优化

### 9.1 先建立时延分解

分析时延从首个有效I/Q进入分析模块计至相应频域结果产生，包含8192点窗口积累。另记发布时延；PC文件上传、UDP接收和GUI刷新作为独立端到端指标，不混入已有硬件计数。

在仿真中标记首输入、末输入、FFT首输出、FFT末输出、频谱bank就绪、扫描完成和结果发布。100MSPS完整8192点窗口积累约81.92µs；精确周期定义说明端点是否包含首周期。

若需要硬件诊断计数，采用验证分支和明确接口。不同域的计数须换算到统一时间基准，计入跨域观察的不确定性；不能直接相减两个不同频率的计数值。

### 9.2 候选：每拍扫描4点

现有频谱扫描每拍2点，8192点约4096拍，在125MHz下主体约32.768µs。每拍4点主体理论约16.384µs；仅该项的理想节省约16.384µs，不能声称系统总时延减半。

需要同时修改：RAM分bank、读延迟对齐、四点前缀和、99%边界选择、同峰规则、帧尾、双bank占用及1024点快照分组。检查BRAM实际映射，不能只按总存储位数估算资源。

验收要求：原全部数值逐位一致；首末频点、边界落在四点内各位置、零功率、等峰、连续bank切换和快照测试通过；采样吞吐不下降，正常采集无丢失，时序通过。

目标争取最大分析延迟低于190µs，实际以同一输入矩阵的最大值为准。若只节省少量时间却明显压缩时序或资源余量，则不纳入默认版本。未完成测量前不承诺该数值。

## 10. S6：可选125MSPS档位

仅在S1～S4完成且100MSPS版本已封存后开始，独立分支实施。

必须联动检查真实采样时钟、FFT服务速率、FIFO容量、频率换算、时间戳、门限时间含义、元数据、主机、GUI、黄金模型和全部验证脚本。不能只改采样率寄存器或显示文字。

125MSPS/8192点的频点间隔为15.2587890625kHz。若时间戳时钟也改为125MHz，2ms对应250000拍；若未改则按原时间基准计算。32点零确认在100MSPS为320ns，在125MSPS为256ns；按“点”还是按“时间”维持行为必须明确。

125MSPS输入配125MHz FFT意味着理论余量更小。先通过持续输入仿真检查ready、FIFO水位、窗口切换和长期服务率，再决定是否需要提高FFT时钟；有限FIFO不能解决长期平均服务率不足。

完成实际IP/板级工程重建、软件和BOOT生成、有限及60秒连续采集、宽带与边界测试、SD及冷启动验证后才能宣称125MSPS达成。成功后分开发布两个档位，失败时保留100MSPS默认启动包。

## 11. 代码修改位置与交付组织

以下新增路径是拟定设计，当前尚不存在；实施时可按职责合并，避免为了目录完整而增加空脚本。

| 位置 | 计划职责 |
|---|---|
| `tests/make_golden.py`及拟新增`tests/fft_reference.py` | 原脚本兼容；可复用AMD位精确模型接口 |
| 拟新增`tests/generate_qualification_vectors.py` | 参数化频率、幅度、噪声、偏置和宽带信号 |
| 拟新增`data/qualification/` | 新波形清单、小型确定性样例；大量矩阵可生成到build |
| 拟新增`scripts/validate_measurements.py` | 用显式清单运行新增矩阵，保存独立报告 |
| `scripts/verify_board_capture.py` | 如需扩展，保留原黄金验证语义，新增参数必须显式 |
| `host/iq_client.py`、`host/monitor.py` | 必要的门限设置、元数据与误差显示 |
| `rtl/spectrum_measure.sv`、`tests/tb_spectrum_edges.sv` | 仅S5确定收益后修改四点扫描及相关测试 |
| `scripts/build_board.tcl`、`create_fft.tcl`等 | S4实现策略；S6真实时钟与IP配置 |
| `scripts/record_build_stage.py`、打包/验收脚本 | 纳入新增输入、工具、报告和原始记录的散列 |
| 拟新增`reports/第二阶段测量与性能报告.md` | 唯一第二阶段总结，链接各矩阵结构化记录 |

新增任意输入参考不应依赖导入时执行整套黄金生成脚本；提取纯接口并明确初始化/释放C模型资源。旧16组合结果不变是重构的兼容检查。

保留当前README总入口和验收报告；不新增第二份PROJECT_STATUS或复制大量README。失败实验放本地生成目录，保留有价值的结论；许可证、历史基线与原始验收证据不作垃圾删除。

## 12. 验证等级与执行入口

### 12.1 按改动选择验证范围

| 改动 | 必须执行 |
|---|---|
| 仅方案、报告 | 文件、链接和指标一致性检查，不重烧板 |
| 新向量、参考与测试脚本 | 参考自身边界验证、旧参考兼容、新样例有限板测；刷新受影响的来源记录 |
| 主机/协议/固件 | 主机协议回归、默认配置兼容、GUI和对应板测；固件变化重新构建ELF/BOOT |
| RTL、约束、实现策略 | 完整仿真、布局布线、软件/BOOT一致性、原完整板测及新增矩阵 |
| FFT配置、采样时钟 | 重建IP与板级工程、全部参考和测试，禁止只增量修改显示 |

现有来源检查把tests/host纳入仿真输入，并把部分主机/固件文件纳入实板证据。改动后应运行对应验证入口；不能只更新SHA字段令检查变绿。扩大清单时同时更新打包范围和工具入口。

### 12.2 当前可用命令

下面列出已有入口，用于实施阶段；本次写方案不执行构建、烧录或采集。每条命令成功后再执行下一条，Windows原生命令需检查`$LASTEXITCODE`。输出目录必须使用新名字。

```powershell
Set-Location -LiteralPath 'D:\fft\iq_analyzer'
$iqPython = 'C:\Users\LENOVO\AppData\Local\Programs\Python\Python312\python.exe'

# 读取现有构建和完整板测证据，不访问或重新烧录开发板。
& $iqPython scripts/package_validated.py --check

# RTL改变后：先仿真；时钟/IP不变时才可选择现有增量硬件入口。
.\scripts\run.ps1 -Action Sim
.\scripts\run.ps1 -Action RebuildHardware
.\scripts\run.ps1 -Action Software
.\scripts\run.ps1 -Action Package

# 仅在候选构建通过后执行；此步会通过JTAG替换当前运行程序。
& $iqPython scripts/deploy_board.py --board 192.168.1.10
.\scripts\run_network_validation.ps1 -Board 192.168.1.10 -Python $iqPython -Suite smoke
.\scripts\run_network_validation.ps1 -Board 192.168.1.10 -Python $iqPython -Suite full
```

FFT/时钟改动需要显式重新生成IP和板级工程，先运行`scripts/create_fft.tcl`重建FFT IP，再运行`scripts/run.ps1 -Action Hardware`重建板级工程，并重新执行参考、仿真、软件和启动包验证；不能假定已有工程上的`All`自动重建全部已存在IP。新增矩阵脚本尚待实现，本文不提供伪装成现成工具的命令。

板测时GUI与脚本只运行一个采集控制端。SD维护使用[维护操作说明](scripts/maintenance/操作说明.md)，新候选通过前保留当前启动文件；JTAG下载不会自动更新实体SD。

## 13. S7：阶段验收与交付

### 13.1 测量能力交付（不改变硬件时）

- [ ] 原黄金数据和关键参考兼容；新参考支持明确的样例清单。
- [ ] 频率/幅度扫描输出完整误差表，不只输出PASS。
- [ ] 带噪、偏置测试给出成功范围、失败范围、样本数与原因。
- [ ] 宽带候选按实际99%带宽比较，最宽稳定档完成60秒连续。
- [ ] 新数据绑定输入、参考、实际硬件版本及构建身份；旧板测报告保持原样。
- [ ] 报告明确哪些功能新增、哪些只是扩大验证；不把测试扩展宣传成电路提速。

### 13.2 硬件候选交付（S4～S6发生变更时）

- [ ] 四项赛题门槛继续满足，接口版本/能力位与实际变化相符。
- [ ] 原功能与新功能仿真通过，setup/hold、CDC、DRC通过。
- [ ] 原完整板测、新增矩阵、压力与恢复通过；GUI显示实际参数。
- [ ] 新bit/XSA/FSBL/ELF/BOOT相互一致并保存SHA-256。
- [ ] SD写入及读回校验、复位启动、用户协助物理冷启动后采集通过。
- [ ] 旧版本可回退，默认启动包仅替换为完整验收通过的候选。
- [ ] 新完整交付包纳入所有新增参考、脚本、原始记录和冷启动补充证据，校验通过。

### 13.3 参赛材料

按20/40/30/10分结构整理调研选型、自研RTL及仿真、实板结果、创新与优化。展示每项优化的前后指标、资源和代价，不预测具体得分。

准备一页硬指标表、一页适用范围表、原理框图、关键仿真波形、实际接线照片和演示视频。演示顺序建议为：冷启动 → 正负频点 → 幅度变化 → 两种测长对照 → 宽带信号 → 连续完整性 → 优化前后对比。

硬件照片、队伍信息及官方视频格式需参赛方补充；不以软件截图代替实物照片。未使用计量校准设备时，时间/频率基准注明标称值和交叉检查方式。

## 14. 风险、停止条件与下一项具体工作

| 风险 | 处理方式 |
|---|---|
| 新向量破坏原固定16组合/SD索引 | 独立清单与数据目录，原入口兼容回归 |
| 噪声背景让数字零模式无法分帧 | 明确不适用，门限模式统计性能，不伪造完整长度 |
| 宽带越界、削顶或循环接缝虚增带宽 | 检查频带保护、量化溢出、稳态/边沿窗和实际输入参考 |
| 只匹配参考却没有验证设计目标 | 位精确一致性与物理/算法误差分两套结果 |
| 并行扫描导致BRAM映射或时序恶化 | 先估资源并仿真，再独立实现；不划算则保留基线 |
| 提频后FIFO长期积压 | 持续吞吐仿真和板测证明服务率，失败即回退 |
| 修改代码但保留旧PASS | 来源散列门槛阻止发布，重做受影响验证 |
| 采集成功但未证明SD冷启动版本 | 独立记录BOOT写入、读回、启动方式和启动后数值结果 |

出现数值不一致、负时序、正常模式丢记录、错误标志被忽略、参数未真正写入硬件或版本身份不明确时，候选不得替换当前稳定版本。性能优化失败不影响已达标版本继续使用。

**下一项具体工作：先完成S0和S1的参考接口设计，生成少量频率/幅度样例并做有限采集。** 先证明新测试工具可靠，再扩展噪声和宽带矩阵；此阶段无需重烧FPGA或更新SD。
