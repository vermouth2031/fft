# 第四阶段优化实施方案：比赛口径、宽带验证与性能提升

编制日期：2026-09-22。基线提交：`f871b999d642e7d4a76a070b7b48b48d3790073c`。

本文是下一轮工作计划，新增目标尚未实现。本次用户请求是制定详细方案；不因本文中的实施步骤而自动启动硬件修改、板测或SD更新。赛事要求依据[赛题整理](../赛道2_FPGA设计及应用赛题整理.md)，差距依据[逐项达标分析](../比赛要求逐项达标分析_20260922.md)。

## 1. 下一轮的总体选择

当前四项数值硬门槛已经达到，继续投入应优先提高比赛认可的确定性和可验证的竞争指标。本轮按以下顺序推进：

1. 统一输入、帧长、频点、带宽、计时和IP使用的声明，整理最终参赛材料。
2. 在现有100MSPS硬件上，扩展真实通信信号的带宽验证，优先争取90MHz级实际99%占用带宽证据。
3. 在隔离候选中评估八路扫描，把最大分析延迟进一步压到178µs以内；175µs附近仅作争取方向。
4. 先完成采样率参数化和时序定位，再评估可实现的小幅提频；125MSPS作为独立高风险档位，不承诺成功。
5. 按赛事实际定义决定是否增加载频估计、协议帧识别、DC补偿或外部输入，避免无依据地扩大项目。
6. 每个候选保留晋升或拒绝证据，最终交付一个稳定默认版本和确实通过验收的可选档位。

预计收益是工程目标，不是赛事得分预测。题面没有各性能指标的加分函数，不能保证90MHz、175µs或125MSPS对应多少分。

## 2. 已有基线与保护范围

| 项目 | 当前已验收事实 | 下一轮不得混淆的内容 |
|---|---|---|
| 输入格式 | I/Q各16bit | 不等于ADC的16位有效精度 |
| 输入/FFT时钟 | 100MSPS源，125MHz FFT | FFT时钟不是输入采样率 |
| FFT | 8192点，AMD IP | 不是自主FFT内核 |
| 最大分析/发布 | 183.41/183.75µs | 不包括电脑上传、网络往返和GUI绘图 |
| 带宽 | 最宽已测档约81～82MHz的99%占用带宽 | 100MHz频轴跨度不是已测100MHz信号带宽 |
| 5dB测长 | 独立496/500正常；相同历史输入97/100 | 不保证0dB、大DC或噪声漂移 |
| 时序 | setup/hold 0.542/0.016ns | 正裕量不等于可以直接125MSPS |
| 资源 | LUT12866，FF20685，BRAM92.5/140，DSP53 | 新缓存及扫描分组按实现报告计算 |
| 验收 | 1653组矩阵、96组兼容检查、36组基础板测、SD及冷启动 | 算法数值PASS不等于所有输入均检出 |

冻结包：`release/phase3-complete-20260922.zip`，SHA-256：`1c2dc16ff59d063ae871dcf3c9f03617661b33c34d306a3e6a08da0f6deeb84c`。

下一轮从该提交建立独立分支/工作树，建议名`optimization/phase4-competition-performance`。保留现有包、bit/XSA/ELF/BOOT、原始采集和报告，使用新目录保存候选；不得把新产物放入旧PASS清单或重写旧包。

当前尚有工具工作区依赖`D:\fft\iq_phase3_scan`，不能在清理时直接删除；先解决实际Vitis workspace与来源清单的迁移和复现，再处理旧目录。

## 3. 阶段划分与优先级

| 阶段 | 工作 | 性质 | 交付与验收 | 粗略投入 |
|---|---|---|---|---|
| Q0 | 冻结基线、赛事口径、样本规划 | 必做 | 来源清单、口径状态表、实验规格 | 0.5～1人日 |
| Q1 | 最终材料、演示入口和参数解释 | 必做 | 数据一致的报告、演示流程、证据入口 | 1～2人日 |
| Q2 | 90MHz级宽带通信信号验证 | 必做实验 | 新带宽成绩或未达目标的完整结论 | 2～3人日 |
| Q3 | 构建身份、速率和吞吐可观测性 | 新RTL候选的共同基础 | 可读构建ID、时钟/单位定义、计数与错误证据 | 1～2人日 |
| Q4 | 八路扫描降低延迟 | 条件性候选 | ≤178µs且无数值/吞吐退步，或拒绝报告 | 2～4人日 |
| Q5 | 采样率参数化及提频 | 必做可行性研究、条件性实现 | 小幅提频档或125MSPS完整验收，或停止依据 | 初筛2～3人日；重构另估 |
| Q6 | 频点、帧长和稳健性补强 | 基线评估必做；新算法按需求进入 | 明确适用范围，必要时增加独立模式 | 单分支2～5人日 |
| Q7 | 完整回归、启动、交付和演示 | 所有晋升候选必做 | 新验收报告、独立包和真实演示材料 | 1～2人日 |

上述是工程投入估计，不是日历承诺；布局布线和完整实板矩阵另有工具等待。若没有足够截止时间，优先完成Q0/Q1/Q2和当前稳定版本的演示交付，不强行进入125MSPS、外部接口或自主FFT重构。

## 4. Q0：赛事定义和可继续工作的边界

建立`reports/phase4_requirements.json`，每项包含题面原文、当前解释、是否收到赛方确认、确认来源和日期、受影响功能。没有回复时保持“未确认”，不能由AI代填已认可。

| 问题 | 暂用定义 | 什么情况下改变实现 |
|---|---|---|
| 是否接受板内回放？ | 预装载后PL每拍接收一对IQ | 明确要求外部连续新数据时，才进入对应输入接口分支 |
| 是否允许厂商FFT IP？ | AMD FFT内核，自有测量/控制RTL | 明确要求自主FFT时，启动独立架构项目和重新估时 |
| 帧长度是什么？ | 突发包络的样本区间 | 指定协议帧、符号数或字节数时，先取得帧格式 |
| 频点是什么？ | 谱峰，同时单列带宽中心 | 要求调制载频时，增加该信号模型下的估计与验证 |
| 带宽、计时如何定义？ | 99%占用带宽；PL首IQ至板内结果 | 指定3dB带宽或端到端时间时，增加独立测量字段和测试 |

本阶段只准备一页确认清单，由参赛者提交给赛方；未获用户明确要求，不自动发送邮件或其他消息。等待口径答复不阻塞基线冻结、文档整理、参考工具及独立实验；会改变合规结论的未知条件不得当作已获认可。

冻结时还应保存当前源码、工具版本、实际IP配置、时钟约束、关键报告和输入/产物散列。核对历史回归中的失败工况，不从下一轮测试清单中悄悄删除。

## 5. Q1：把已有成果转化为可审阅的参赛材料

### 5.1 统一最终设计说明

新增《最终电路设计说明》，以实际工程为准，涵盖需求对照、系统框图、时钟域、定点位宽、回放接口、FFT配置、幅度/帧长/频点/带宽定义、延迟计时点和异常标志。

早期调研文件作为历史方案保留，不把125MSPS目标改写为已实现。最终说明明确“原双路扫描→当前四路扫描→本轮候选八路扫描”的版本关系；把`D:/精英赛`等旧绝对路径整理成有效的交付相对链接。

列出自有RTL、AMD FFT/XPM、板级定义、驱动及参考模型的职责。答辩应能解释为什么使用PL流水处理、为何保留100MSPS、为什么以99%功率分位点定义带宽，以及哪些失败决定了不增加某些模式。

`rtl/spectrum_measure.sv`顶部仍有旧的even/odd双路注释。应在下一次硬件来源刷新时修正；不要只为修改这一条注释就将旧仿真散列手工改成新值。

### 5.2 演示入口

GUI/CLI统一展示以下信息：输入来源、每路位宽、IQ对/秒、FFT点数、实际窗函数、检测模式、门限来源、峰频、带宽中心、99%带宽、分析延迟和错误状态。

“载频”字段仅在确有相应算法及验证时使用；板内延迟与电脑端耗时分别命名。演示预设分为零背景测长、带噪robust、单音频点和宽带通信信号，每项绑定具体向量/参考/配置，不隐藏失败标志。

建议制作一套10～12页的可裁剪答辩内容：需求、方案选择、架构、时域、频域、流水计时、仿真、板测、优化对照、适用边界、复现。页数是内部建议，最终以赛事要求为准。真实接线照片、实物运行画面和录像需要实际拍摄，不能用生成图替代。

验收：所有展示数字有报告或原始数据来源；新机器解压后文档链接有效；操作能从当前SD启动进入演示；GUI控件与实际寄存器一致；过期目标不会显示为当前成绩。

## 6. Q2：优先在100MSPS下扩大已验证的实际带宽

### 6.1 为什么先做这一项

现有81～82MHz由已选择的RRC-QPSK波形及99%定义共同决定，不是已经证明硬件超过82MHz就失败。赛题允许自选数字基带类型，因此先验证具有更多有效高频分量的真实通信波形，比直接改时钟更容易判断收益。

这项工作的成果应称为“扩展带宽验证范围”，不能在RTL不变时称为电路带宽已提速。保留原RRC矩阵，用新信号补充，不用纯噪声或人为插入两端少量大功率线谱代替通信信号来抬高成绩。

### 6.2 新通信波形与规格

优先增加具有数据子载波和循环前缀的OFDM合成向量：

- 示例初始规格为4096点OFDM IFFT、CP=256点，子载波调制先QPSK，16QAM作为补充；8192点分析FFT保持不变。OFDM符号长度不必等于分析窗长，应覆盖各种对齐相位。
- 连续、对称分布的数据子载波，显式规定DC空置、导频、两端保护带；记录活跃子载波数及对应频率。按100MSPS分别设计约85/90/95MHz的活跃频带跨度候选。
- 活跃频带跨度是设计参数，99%占用带宽是测量结果。90MHz活跃跨度不保证99%带宽≥90MHz；实际门槛必须由量化IQ和完整频谱计算。
- 每个有限输入包含明确静默区、完整或显式裁切的符号、尾部处理方式；在32768点限制内声明全部裁切。连续回放首尾设置已知静默接缝，计时不靠漏分析接缝取得收益。
- 记录量化前后功率、PAPR、分量峰值和余量。用训练集确定统一增益并冻结；独立集如溢出必须计为失败，不能偷偷换seed或按每个样本峰值重新挑选增益。

先以相同量化IQ和相同窗做浮点FFT/CDF参考，再由AMD位精确模型生成硬件参考。逐条比较定点数值；浮点差异作为算法误差单列，不能把硬件与浮点有差异一律判为RTL错误。

### 6.3 数据与板测矩阵

建议预留全新seed区间：训练210000～210099，独立验收211000～211999，后续备用212000～212999。实施前核对整个项目没有使用过这些编号，并冻结生成器版本、随机算法和seed→IQ散列；仅换seed编号不能证明独立性。

初始有限矩阵：3档设计跨度×2种子载波调制×2种分析窗×20个独立seed，共240例。每例至少4个FFT窗，标记静默、突发边沿、CP/符号边界和内部窗口；相同seed跨工况属于成组样本，统计不当作完全独立。

追加24例保护带/频移边界测试，明确区分未跨Nyquist与跨界输入。先保留原算法对跨界的限制，不让跨界频谱把线性CDF宽度虚增为新成绩。

通过有限验收后，分别选“最宽合格档”和“稳定演示档”：每档矩形/Hann各10秒，演示窗再60秒；候选最终冻结后增加一次300秒稳定性测试。全部原始结果检查完整性，重复回放时间不计为独立随机数据时长。

### 6.4 接受标准与停止条件

- 第一目标：至少一个预先定义的通信档位，在独立验收中所有声明有效的内部窗口，实际99%占用带宽最小值≥90MHz；边沿/静默窗口另外报告，不删除或混入该分母。
- 次级目标：若90MHz达成且仍有保护带余量，探索95MHz级；不得用跨界折叠、数值溢出或异常标志换取大数值。
- 当前100MSPS硬件的正常最大分析/发布保持≤190µs；输入、序号、时戳、FFT溢出、丢记录均满足原检查；每个窗口与冻结定点参考一致。
- 固定工作量先完成240+24例，不因某个独立seed不理想而反复调参。若改波形参数，原独立集转回归，启用未看过的备用集。
- 未达90MHz时交付实际min/median/P95/max和原因，保留81～82MHz稳定档。90MHz是本轮工程目标，不是赛题合格线。

## 7. Q3：为新硬件建立更可靠的身份和吞吐证据

### 7.1 构建ID和能力字段

在下一次实际RTL候选中增加只读构建ID及能力/格式版本。构建ID由构建前的源码、IP配置、约束和时钟配置生成，可采用截断的128位摘要；生成文件自身不参与输入摘要，避免循环依赖。最终清单同时记录完整输入摘要和最终bit SHA-256。

构建ID不是bitstream自身散列，也不等于安全认证。它的目的只是区分接口版本相同而实现不同的镜像，辅助JTAG、SD和采集元数据绑定。

新增寄存器地址先审查现有译码，不能占用已有字段。版本号和能力位同步贯通固件、CLI、GUI、参考与打包工具；老客户端对不支持的新格式应明确拒绝，不静默解码为旧数据。

### 7.2 计数与时间单位

记录源端发出/实际接收样本数、FFT输入输出窗数、结果数、FIFO溢出/欠读以及输入FIFO高水位。跨域计数采用安全同步或握手快照，不直接读取变化中的多位二进制计数器。

从连续采集证明：输入计数与窗口数相符、首样本位置每窗递增8192、同一速率下源时间戳间隔正确、FIFO水位无持续增长、正常情况下错误和丢记录为0。故障注入另列，不把故障期最大延迟混入正常成绩。

数据格式应明确`sample_rate_hz`、`timestamp_clock_hz`、`fft_clock_hz`及延迟周期属于哪个时钟。当前源与计时时钟均100MHz；下一轮若拆域，不能继续假设时戳频率必然等于采样率。

Q3是新RTL分支的共同基础；仅做Q1文档或Q2现有硬件测试时，不为加构建ID而提前重做整套硬件。

## 8. Q4：八路扫描的有界低延迟实验

### 8.1 预期收益

当前四路扫描主体需要2048拍；八路可将主体降到1024拍，125MHz下理想减少8.192µs。新增前缀和、比较及快照流水会吃掉部分收益，系统总延迟必须重新测量，不能称为减半。

本轮晋升目标为同一原始回归集合最大分析≤178µs，争取约175µs；至少比183.41µs改善5µs。达不到收益门槛且资源明显上涨时，保留四路扫描。

### 8.2 结构与验证

`rtl/spectrum_measure.sv`每个bank按q低3位分为8组，每组1024×48bit，仍保留双bank。每拍8点前缀和分级流水，按最早满足CDF阈值的q确定边界，等峰仍取最小q，ROI和零功率语义保持。

每8bin取最大值的1024点快照现在对应一个八点组，但仍要保留请求、所属窗口和跨bank规则。不得为了减少时延改成抽取单点、缩小ROI或减少参与CDF的FFT点数。

专项测试至少包含CDF落在8个位置、首末bin、同峰跨组、ROI边界、零能量、连续bank切换、快照请求各状态、输入气泡、有限输入结束。全部524288点核心参考及原帧长/AXI/协议测试继续通过。

### 8.3 实现门槛

- 相同真实时钟约束下，目标setup≥0.10ns、hold≥0，CDC Critical=0、无未约束内部端点、DRC无未解释错误。
- BRAM占用≤75%作为内部预警线，LUT/FF增量与总时延收益一并报告；超线需重新评估组合方案，不把RTL数组位数当实际BRAM数量。
- 做一次明确的布局/流水实现和一次针对已识别瓶颈的修正；不要无限尝试随机实现直到挑到漂亮结果。
- 完整重新构建相互匹配的bit/XSA/ELF/BOOT，完成原矩阵与Q2合格集、60秒持续测试后才可晋升。静态时序通过不能替代实板。

## 9. Q5：采样率参数化与提频路线

### 9.1 先参数化，先在100MSPS证明不退步

排查并消除业务语义中的硬编码100000000、125000000及固定Fs/N比例，包括`rtl/result_builder.sv`的390625/32、外设寄存器、固件、时间换算、GUI、FFT参考、向量生成、采集验证和报告。

优先使用构建期合法速率档位及精确有理数换算，避免首次版本引入任意动态切频。固定8192点时，频率、带宽和时间舍入规则由独立整数参考给出；100MSPS档须与原数据逐位一致。

检测参数默认保持“样本数”语义并显示对应时间。例如32点在100MSPS为320ns，在125MSPS为256ns。若另选保持物理时间，应新建明确档位并重新验证，不能自动换算后假装算法未变。

### 9.2 小幅提频先做可行性筛查

第三阶段固定布线预算显示源域直接从10ns改8ns约为−1.458ns，因此不能直接宣布125MSPS可用。可先筛查接近102～104MSPS的合法时钟组合，用于争取高于最低门槛的实际速率，但不能预设工具一定能生成这些精确频率。

以源域0.542ns旧slack做不改布线的示意预算，104MHz周期约9.615ns，剩余约0.157ns；这只是同域路径初筛，不能证明跨域路径、新时钟相位、抖动及重新布线会通过。实际频率由PS/PLL/MMCM合法配置、生成时钟约束和实现报告确定。[AMD UG585时钟资源](https://docs.amd.com/r/en-US/ug585-zynq-7000-SoC-TRM/Mixed-Mode-Clock-Manager-and-Phase-Locked-Loop)

### 9.3 125MSPS作为独立重构候选

先按源域、AXI/结果域和FFT域分类最差路径，记录逻辑级数、扇出、布线比例、跨域关系。由证据决定是否拆分100MHz控制域与125MHz采样域，以及是否提高FFT服务时钟；拆域会增加CDC、队列和计时处理，不能仅改常量。

可评估125MSPS输入配156.25MHz级FFT作为维持原1.25服务时钟比例的候选，但当前FFT固定布线到6.4ns周期的预算约−1.008ns，仍需要真实重建和优化。125MSPS配125MHz FFT也不能凭“一拍一数据”认定持续吞吐足够，应检查实际帧间隙和平均服务率。

125MSPS/8192≈15258.789窗/秒。必须长期满足到达率不大于服务率，FIFO水位不能线性增长；有限缓存不能补偿平均吞吐不足。位宽、FFT点数、全部功率样本及原始测量范围不得为提频而缩水。

所有速率档先仿真实际时钟、有效气泡、有限输入和背压，再做实现；通过后才进行10/60/300秒板测。原数据按不同Fs解释与保持同一物理信号重新采样是两种测试，分别保留，不把频轴乘1.25当真实带宽升级。

### 9.4 是否切换Realtime FFT

默认继续Non-Realtime。只有在供数架构能保证每次FFT请求都拿到有效样本、输出端无需停顿时，才建立Realtime独立分支。AMD明确说明Realtime帧开始后不再用输入TVALID空拍等待，缺数可能复用旧数据并破坏结果。[AMD PG109供数规则](https://docs.amd.com/r/en-US/pg109-xfft/Controlling-the-FFT-Core)

理想连续输入下，单帧预存下限可由`N×(1−Fs/Ffft)`估算；它不覆盖突发供数、时钟相位或控制延迟。缓存占用、帧启动等待、输入结束和双bank调度都计入首IQ至最终结果的总时延，不能只比较FFT输出段。复位后的配置必须在第一帧输入前正确完成。[AMD PG109空闲配置](https://docs.amd.com/r/en-US/pg109-xfft/Applying-a-New-Configuration-While-Idle)

### 9.5 提频候选的晋升标准

- 真实时钟配置、实际元数据、独立参考和硬件计数一致；不靠只改元数据提频。
- 目标setup≥0.10ns、hold≥0，完整时序/CDC/DRC通过；100MSPS兼容档保持原测量口径。
- 原功能、幅度/频率边界、robust独立数据、Q2宽带、持续吞吐、GUI及异常恢复均通过。
- 至少满足赛题≤2ms；本轮工程保护线为正常最大分析≤190µs。若要同时宣传“提频且降延迟”，必须在同一候选上证明≤183.41µs，不能拼接两个版本的最好值。
- 最多先进行两个有明确架构差异的候选实现；若关键路径仍显著负裕量、服务率不足或资源超出可解释范围，停止该轮提频，交付失败路径和下一步所需重构，不无限延长。

## 10. Q6：测量语义和稳健性补强

### 10.1 频点：先验证现有中心估计，再决定加算法

先用明确载频的单音、RRC-QPSK和OFDM做离线对照，分别统计谱峰、99%带宽中心与真实频移的差值。初始基线矩阵可用3种信号×5个合法频移×3档SNR×20个新seed，共900例，主要离线执行；保证设计频带不越过Nyquist边界。

训练seed建议220000～220099，独立221000～221999，备用222000～222999，冻结前核对未用。比较质心、带宽中心、已知导频或协议允许的方法时，必须声明各自假设；多信号、非对称频谱、DC和强干扰不应输出伪精确载频。

单音亚bin插值可作为独立候选，内部目标为声明幅度、SNR≥20dB且远离频带边缘时P95误差≤0.1bin；至少验证±半bin附近、等峰、低幅和失效标志。该指标不适用于宽带调制信号。

宽带载频估计不直接沿用0.1bin目标。先采用预先声明的探索容差`max(2bin,0.5%真实99%带宽)`评估，明确它不是赛事标准，也不能保证满足尚未公布的载频精度要求。若赛方给出容差，应在看新独立集之前替换规格。

只有算法在独立数据上达到冻结目标，才考虑RTL增加累加/除法或新字段。主机离线估计保留为离线证据，不能说成板内已在183.41µs内得出。不得用1024点“8bin取最大”快照代替8192点完整功率谱计算高精度质心。

### 10.2 噪声证据：扩大唯一数据量，不重复调原留出集

现有独立纯噪声仅0.08388608秒。下一轮先准备至少1秒不重复的噪声IQ，噪声幅度分层，并区分统计独立块和过滤器相关样本；离线对冻结robust检测器评估虚警与边界。

100MSPS下1秒I16/Q16为400MB，32768点一块需至少3052块。若全部逐块实板上传，每块控制开销会使测试明显长于1秒，先测小批吞吐再决定板测预算。后续如需约3秒唯一数据，至少9156块、约1.2GB；循环同一块3秒不等价。

零事件时常用的95% Poisson上限约为3/T次/秒，仅在该事件模型适用时作为说明，不能把重叠能量窗或重复数据自动当作独立事件。分层模型的观察时长和上限分别报告，不无条件合并为一种噪声分布。

已有30/20/10dB≥99%、5dB≥95%的独立正常率作为最低保护线；新算法还应在相同已知回归输入上不劣于当前结果。不得反复利用已见过的第三阶段独立集选参数。

### 10.3 DC和协议帧的进入条件

DC补偿仅在明确需要偏置场景且独立算法收益成立时进入。已有离线125/128正常不是硬件验收；17位差值、33位功率和37位16点能量和需重新设计接口。检测支路去DC不改变原始频谱/幅度口径，合法0Hz信号不能被隐式删除。

若赛事要求协议帧，先给出具体协议、前导、帧结构和长度定义，再设计相关同步/解析。仅自选一个前导的实验模式应叫“自定义帧”，不能冒称支持任意无线通信协议。没有协议定义时，这一分支保持需求状态。

0dB通用高精度测长不是本轮默认必达目标。上一轮M=32/64没有同时满足检出、边界和虚警，除非改变方法或输入模型，不再把扩大窗口作为未经论证的优化方案。

## 11. 外部输入与自主FFT：单独立项的触发条件

若赛方明确要求外部持续数字输入，先确认连接器、电平、源时钟、每拍位宽、引脚预算及允许背压，再选择源同步接口并完成输入约束、CDC及数据发生器验证。DDR长回放仍是回放，不应冒充外部信号实时接收；增加DMA也不自动证明持续吞吐。

如要求模拟接收，另需ADC、射频前端、参考时钟和校准条件，这已超出当前纯数字电路升级范围。在器件/接口未知时不给出购买承诺或虚构可接线方案。

如禁止厂商FFT IP，重新评估自主8192点流水FFT的缩放、旋转因子、资源与误差；外围可复用，内核必须有新的独立参考和实现/实板证据。该工作不能塞进八路扫描或125MSPS的小改动工期。

## 12. Q7：验证、组合和发布规则

### 12.1 不拼接不同版本的最优成绩

建议候选A只改八路扫描，候选B只做参数化/提频；分别通过后再建立组合C。组合C必须重新构建和验收，A的延迟与B的采样率不能共同写成一个未经测试的规格。

保存三条判断：位精确实现是否正确、算法相对真值是否合格、整体速率/延迟是否达到工程目标。异常用例的数值PASS与检出失败可以同时成立，报告必须保留两者。

### 12.2 按改动范围安排验证

| 改动 | 最低验证范围 |
|---|---|
| 文档/统计展示 | 来源绑定、单位、链接、统计分母、与实际界面一致 |
| 只增波形/离线工具 | 生成器边界、浮点和定点参考、留出集、旧向量散列不变、新实板矩阵 |
| GUI/配置入口 | 参数校验、读回、元数据、旧命令兼容、有限板测和实际GUI流程 |
| 谱扫描/构建ID RTL | 模块定向测试、完整核心/AXI/协议仿真、STA/CDC/DRC、原1653矩阵与新增合格集 |
| 采样率/FFT/时钟 | 显式重建IP及全部产物、不同速率参考、FIFO/持续吞吐、全套实板、匹配SD和冷启动 |
| DC/新检测模式 | 位宽极值、独立检测参考、负满幅/截断/短帧、独立噪声/DC板测、协议能力检查 |

每个新矩阵前运行原16组有限用例检查；一次只允许一个板卡控制端。构建或核心配置改变后不得继续沿用旧参考或旧部署身份。原始失败文件保留，不在同目录覆盖重试。

### 12.3 最终通过条件

1. 已进入的分支都有实际完成状态：晋升、拒绝或明确外部阻塞；没有把“准备了脚本”写成“测试完成”。
2. 正常用例没有未解释的丢样、序号/时戳错位、FFT溢出、配置读回失败；注入故障能识别并恢复。
3. 来源、构建ID、bit/XSA/ELF/BOOT、配置、参考和原始记录一致；所有官方硬门槛仍满足所声明口径。
4. 仅在新硬件完整验收后更新SD：保留旧BOOT、完整读回并核验SHA-256，随后SD复位启动和用户实际断电上电后的纯网口验收。软件复位不冒充物理冷启动。
5. 新完整包在新目录生成，验证ZIP CRC和所有文件散列；检查换机器解压后的文档、来源路径和运行说明。保留第三阶段包不变。
6. 最终说明列出实现结果、未晋升实验、适用范围、真实照片/视频的准备状态，不承诺具体比赛得分。

## 13. 计划文件位置与交付物

以下路径是计划，不代表已经存在或已经运行：

| 位置 | 职责 |
|---|---|
| `reports/phase4_requirements.json` | 赛事口径及确认来源 |
| `reports/最终电路设计说明.md` | 与最终实现一致的参赛说明 |
| `scripts/phase4_prepare_bandwidth.py` | 新通信波形规格、seed冻结和参考准备 |
| `tests/generate_qualification_vectors.py`或独立生成器 | 明确OFDM、CP、子载波、量化和溢出规则 |
| `scripts/phase4_frequency_study.py` | 频点/载频基线和候选的离线评估 |
| `scripts/phase4_noise_study.py` | 唯一噪声观察时间、虚警统计和抽样板测 |
| `scripts/phase4_clock_matrix.*` | 合法时钟组合、关键路径和服务率比较 |
| `rtl/spectrum_measure.sv` | 条件性八路扫描，保留测量语义 |
| `rtl/iq_peripheral.sv`、固件、客户端 | 构建ID、能力、真实速率和吞吐观测 |
| `rtl/result_builder.sv`及参考 | 各时钟/速率的精确频率与时间换算 |
| `build/phase4_<日期>/<候选>/` | 独立参考、实现、日志和冻结清单 |
| `captures/phase4_<日期>/<阶段>/` | 新原始板测，不覆盖第三阶段 |
| `reports/第四阶段完整验收报告.md` | 目标、实测、拒绝原因与参赛对照 |
| `release/phase4-complete-<完成日期>.zip` | 最终新完整交付包 |

## 14. 首轮具体执行清单

1. 校验第三阶段冻结包及源码，建立第四阶段分支和来源快照。
2. 建立五项口径状态表与最终设计说明提纲，整理旧路径和过期目标。
3. 完成OFDM生成器、溢出/PAPR检查和离线带宽初筛，冻结240+24例正式规格。
4. 用现有100MSPS硬件完成Q2新矩阵；先形成无需改硬件的明确成果。
5. 冻结构建ID/能力/速率字段，先做100MSPS兼容验证。
6. 在不同候选中做八路扫描与时钟可行性实验，每个实验只改变一个关键因素。
7. 并行于工具等待做频点语义评估及唯一噪声数据研究；只有需求和收益都成立才进入新的算法RTL。
8. 按门槛选择候选、完成组合回归、SD/冷启动及交付，最后更新演示材料中的真实数字。

如果只允许完成一条新增技术主线，优先Q2；若已有足够宽带成绩且强调处理速度，则选择Q4。125MSPS是高收益但高不确定性的后续分支，不能以它尚未实现为由拖延已可交付的稳定项目。
